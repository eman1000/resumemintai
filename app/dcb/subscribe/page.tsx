import DcbSubscribeClient from "./SubscribeClient";

export const metadata = {
  title: "ResumeMint AI — Mobile Billing Subscribe (Demo Advert)",
  robots: { index: false },
};

export default function Page() {
  return <DcbSubscribeClient />;
}
